{% extends 'base.html.twig' %}

{% block page_header %}
    <h1>Teddy creation</h1>
{% endblock %}

{% block page_content %}
    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
